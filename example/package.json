{
  "name": "example",
  "private": true,
  "version": "1.0.0",
  "description": "A couple annotated files to test jsdoc-jsx plugin",
  "scripts": {
    "clean:docs": "rm -rf docs",
    "docs": "jsdoc -c conf.json -d docs",
    "predocs": "npm run clean:docs",
    "preserve:docs": "npm run docs",
    "serve:docs": "http-server -o -p 9000 -c-1 ./docs"
  },
  "author": "Christian Muñoz",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.9.0",
    "jsdoc": "^3.4.0",
    "jsdoc-jsx": "^0.1.0"
  }
}
